task :default => :test_simple


desc "Ejecutar solo las pruebas simples"
task :test_simple do
  sh "ruby test/tc_racional.rb"
end

desc "Ejecutar solo las pruebas simples"
task :test_suma do
  sh "ruby test/tc_racional.rb"
end